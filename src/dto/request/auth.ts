export type RegisterRequest = {
    firstName: string
    lastName: string
    email: string
}

export type SendFileAuthRequest = {
    data: string
}

export type FaceLoginRequest = {
    data: string
}